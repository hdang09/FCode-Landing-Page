const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    compiler: {
        styledComponents: true,
    },
};

// const nextConfig = {
//     serverFileName: 'server.js',
// };

module.exports = nextConfig;
